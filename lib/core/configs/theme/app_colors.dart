import 'package:flutter/material.dart';

/// Common colors used throughout the application.
class AppColors {
  // ─────────────────────────────────────────────
  // Primary
  // ─────────────────────────────────────────────

  static const primary = Color(0xffF59E0B);
  static const primaryLight = Color(0xff0284C7);

  static const primaryDark = Color(0xff38BDF8);
  static const secondaryDark = Color(0xff0EA5E9);

  // ─────────────────────────────────────────────
  // Backgrounds
  // ─────────────────────────────────────────────

  static const lightBackground = Color(0xFFF8FAFC);
  static const darkbackground = Color(0xFF0B0F19);

  static const white = Color(0xffFFFFFF);
  static const black = Color(0xff000000);

  // ─────────────────────────────────────────────
  // Gray / Neutral
  // ─────────────────────────────────────────────

  static const gray = Color(0xffBEBEBE);
  static const lightGray = Color(0xffE0E0E0);
  static const mediumGray = Color(0xff9E9E9E);
  static const darkgray = Color(0xff343434);

  // ─────────────────────────────────────────────
  // Text
  // ─────────────────────────────────────────────
  static const textPrimaryL = Color(0xFF0F172A);
  static const textSecondaryL = Color(0xFF64748B);
  static const textMutedL = Color(0xFF94A3B8);

  static const textPrimaryD = Color(0xFFDFE2F1);
  static const textSecondaryD = Color(0xFFB8C0D0);
  static const textMutedD = Color(0xFF99A3AB);

  // ─────────────────────────────────────────────
  // Status
  // ─────────────────────────────────────────────

  static const success = Color(0xff42C83C);
  static const error = Color(0xffE53935);
  static const warning = Color(0xffFFB300);
  static const info = Color(0xff2196F3);

  // ─────────────────────────────────────────────
  // UI Elements
  // ─────────────────────────────────────────────

  static const borderLight = Color(0xFFE2E8F0);
  static const dividerLight = Color(0xFFE2E8F0);
  static const cardLight = Color(0xFFFFFFFF);

  static const cardDark = Color(0xFF161B26);
  static const borderDark = Color(0xFF232B3E);
  static const dividerDark = Color(0xFF1E293B);

  // Bottom navigation
  static const bottomNavLight = Color(0xFFFFFFFF);
  static const bottomNavDark = Color(0xFF0B0F19);
}
